extends layout

include mixins/_notificationForm.pug

block content
  h1= title
  +notificationForm

block scripts
  script(src=`https://maps.googleapis.com/maps/api/js?key=${process.env.GOOGLE_MAPS_API_KEY}&libraries=places`)
  script(src="/dist/main.bundle.js")
